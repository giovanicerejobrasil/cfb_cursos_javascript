/**
 * Arrow Functions (Funções Lambdas)
 */

const soma = (v1, v2) => {
    return v1 + v2
};
console.log(soma(10, 1));

const nome = n => {
    return n
};
console.log(nome("Giovani"));

const add = n => n + 10;
console.log(add(17));
